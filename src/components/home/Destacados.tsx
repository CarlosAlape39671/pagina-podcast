/**
 * Destacados de Inicio: las publicaciones más recientes de Actualidad (RF-11).
 * STUB — se implementará al desarrollar la pantalla de Inicio.
 */
export function Destacados() {
  return (
    <section aria-label="Últimas publicaciones">
      <h2 className="mb-3 text-xs font-semibold uppercase tracking-wide text-muted-foreground">
        Últimas publicaciones
      </h2>
      <div className="rounded-lg border border-dashed p-6 text-sm text-muted-foreground">
        Destacados (stub) — grilla de últimas publicaciones.
      </div>
    </section>
  );
}
