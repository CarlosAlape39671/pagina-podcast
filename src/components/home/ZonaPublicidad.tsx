/**
 * Zona de publicidad de Inicio: banners administrables por el cliente (RF-09).
 * STUB — se implementará al desarrollar la pantalla de Inicio.
 */
export function ZonaPublicidad() {
  return (
    <section aria-label="Publicidad">
      <div className="grid place-items-center rounded-lg border border-dashed p-8 text-xs font-semibold uppercase tracking-wide text-muted-foreground">
        Publicidad — banner (stub)
      </div>
    </section>
  );
}
