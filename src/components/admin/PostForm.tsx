/**
 * Formulario "Nueva publicación": enlace de YouTube, vista previa, título,
 * descripción y fecha opcional (RF-05). STUB — se implementará en el panel admin.
 */
export function PostForm() {
  return (
    <div className="rounded-lg border border-dashed p-6 text-sm text-muted-foreground">
      PostForm (stub) — pegar enlace de YouTube + vista previa + título, descripción y fecha.
    </div>
  );
}
