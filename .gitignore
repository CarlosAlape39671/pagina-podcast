# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# TypeScript (build incremental)
*.tsbuildinfo

# Variables de entorno (claves) — NUNCA versionar
.env
.env.local
.env.*.local

# Editor / SO
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Diagramas: se versionan los SVG; los PNG se conservan solo en local
# (informes Word / presentaciones) y son regenerables desde el manual técnico.
docs/assets/diagramas/png/
